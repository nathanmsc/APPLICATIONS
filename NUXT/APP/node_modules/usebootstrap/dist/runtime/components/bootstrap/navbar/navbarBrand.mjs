import { defineComponent, h } from "vue";
import { InlineProps, useInline } from "../../../composables/base/useInline.mjs";
import { hProps } from "../../../utils/useProps.mjs";
import {
  AnchorProps,
  useAnchor
} from "../../../composables/html/useAnchor.mjs";
import BsLink from "../../nuxt/bslink.mjs";
export default defineComponent({
  name: "BsNavbarBrand",
  props: {
    ...InlineProps,
    ...AnchorProps,
    tag: {
      type: String,
      default: "span"
    }
  },
  setup(props, context) {
    const inline = useInline(props);
    const Anchor = useAnchor(props);
    const current = {
      class: {
        "navbar-brand": true
      }
    };
    return () => h(
      BsLink,
      hProps(
        current,
        Anchor,
        inline
      ),
      context.slots
    );
  }
});
